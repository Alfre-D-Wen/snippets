{
    "cocotb_isp_0": {
        "prefix": "valid_insert",
        "body": [
            "def valid_insert():",
            "    while True:",
            "        # random.choices(population, weights)",
            "        # return a list, [0] means pop the first element",
            "        yield random.choices([1,0], weights=[0.0, 1])[0]"
        ],
        "description": "cocotb_isp"
    },
    "cocotb_isp_1": {
        "prefix": "ready_insert",
        "body": [
            "def ready_insert():",
            "    while True:",
            "        # random.choices(population, weights)",
            "        # return a list, [0] means pop the first element",
            "        yield random.choices([1,0], weights=[0.0, 1])[0]"
        ],
        "description": "cocotb_isp"
    },
    "cocotb_isp_2": {
        "prefix": "clock_gen",
        "body": [
            "cocotb.start_soon(Clock(dut.clk_in, , units=\"ns\").start())"
        ],
        "description": "cocotb_isp"
    },
    "cocotb_isp_3": {
        "prefix": "agent_i",
        "body": [
            "agent_i = VideoAxisAgent(dut, \"ACTIVE\", \"s_axis\", valid_insert, video_trans)"
        ],
        "description": "cocotb_isp_3"
    },
    "cocotb_isp_4": {
        "prefix": "video_mon",
        "body": [
            "VideoMon(dut, \"output\", valid_signal, data_signal, data_length)"
        ],
        "description": "cocotb_isp_4"
    }
}